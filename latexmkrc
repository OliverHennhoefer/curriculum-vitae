# This project has no labels, references, bibliography, table of contents, or
# other cross-run data. One LuaLaTeX pass is sufficient for both entrypoints.
# Keeping latexmk from repeating the pass leaves margin on Overleaf's free plan.
$max_repeat = 1;
